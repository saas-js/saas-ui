---
id: bulk-actions
scope: props
---

## Props

The `BulkActions` component accepts all `Banner` properties.

### BulkActions

<PropsTable of="BulkActions" only={['title', 'selections', 'children']} />
