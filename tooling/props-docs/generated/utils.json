{
  "ExtendedFocusOptions": {
    "isActive": {
      "type": "(element: FocusableElement) => boolean",
      "required": false,
      "description": "Function that determines if the element is the active element"
    },
    "nextTick": {
      "type": "boolean",
      "required": false,
      "description": "If true, the element will be focused in the next tick"
    },
    "preventScroll": { "type": "boolean", "required": false },
    "selectTextIfInput": {
      "type": "boolean",
      "required": false,
      "description": "If true and element is an input element, the input's text will be selected"
    }
  },
  "PanSessionOptions": {
    "threshold": { "type": "number", "required": false },
    "window": { "type": "Window", "required": false }
  }
}
