---
title: Spacing
description: Spacing scale shipped with the Saas UI preset
---

The spacing scale is a 4px grid (`1` = `0.25rem`) multiplied by
`--scale-factor`. Size tokens reuse the same steps, so `p="4"` and `boxSize="4"`
stay in sync when you change scale.

<SpacingTokenDoc />
