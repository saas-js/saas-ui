---
title: NProgress
description: Show feedback when switching pages and content is loading in the background.
---

NProgress can be helpful in single-page apps (SPA) to give a visual clue to users that
something is happening when browsing, in case a page isn't preloaded and may take a moment to load.

<ComponentLinks
  github={{ package: 'saas-ui-nprogress' }}
  npm={{ package: '@saas-ui/nprogress' }}
/>

## Import

```ts
import { NProgress, NProgressNextRouter } from '@saas-ui/react'
```

## Usage

### Basic

```jsx
<Box position="relative" overflow="hidden" height="10px">
  <NProgress isAnimating position="absolute" />
</Box>
```

### Bar color

```jsx
<Box position="relative" overflow="hidden" height="10px">
  <NProgress isAnimating colorScheme="teal" position="absolute" />
</Box>
```

### Trigger

```jsx
function App() {
  const [isAnimating, setAnimating] = useState(false)
  return (
    <Stack>
      <Box>
        <Button onClick={() => setAnimating(!isAnimating)}>
          Toggle progress
        </Button>
      </Box>
      <Box position="relative" overflow="hidden" height="10px">
        <NProgress
          isAnimating={isAnimating}
          colorScheme="teal"
          position="absolute"
        />
      </Box>
    </Stack>
  )
}
```

### Use with Next Router

```jsx live=false
// _app.tsx
function App({ Component, pageProps }) {
  return (
    <Layout>
      <NProgressNextRouter />

      <Component {...pageProps} />
    </Layout>
  )
}
```

## Props
