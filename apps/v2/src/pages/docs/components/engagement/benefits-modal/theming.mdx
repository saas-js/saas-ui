---
id: benefits-modal
scope: theming
---

## Theming

The `BenefitsModal` component composes a `Modal` component. This means that all of the `Modal` props can be passed to the `BenefitsModal` component.

- [Modal theming documentation](https://chakra-ui.com/docs/overlay/modal/theming)
