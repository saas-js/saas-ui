---
id: bulk-actions
scope: theming
---

## Theming

The `BulkActions` component composes a `Banner` with the `floating` variant by default.

Theming can be customized by changing the [Banner theme](/docs/components/feedback/banner).
