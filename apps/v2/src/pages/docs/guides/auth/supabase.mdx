---
title: Supabase
package: '@saas-ui/supabase'
description: Integrating Supabase authentication with Saas UI.
---

<ComponentLinks
  github={{
    package: 'saas-ui-supabase',
  }}
  npm={{ package: '@saas-ui/supabase' }}
/>

This integration allows you to connect Supabase with the Saas UI auth screens and provider.

Supported authentication strategies are:

- Magic link
- Password
- Social login (Oauth)

## install

```
yarn add @saas-ui/supabase @supabase/supabase-js
```

## Import

```js
import { createAuthService } from '@saas-ui/supabase'
```

## Usage

If you haven't done so already install `@supabase/supabase-js`.

```bash
yarn add @saas-ui/auth @saas-ui/supabase @supabase/supabase-js
```

### Setup AuthProvider

```tsx
import { createClient } from '@supabase/supabase-js'
import { AuthProvider } from '@saas-ui/auth'
import { createAuthService } from '@saas-ui/supabase'

const supabaseClient = createClient(
  process.env.SUPABASE_URL,
  process.env.SUPABASE_KEY
)

function App({ children }) {
  return (
    <SaasProvider>
      <AuthProvider {...createAuthService(supabaseClient)}>
        {children}
      </AuthProvider>
    </SaasProvider>
  )
}
```

That's it! Check the [Auth documentation](/docs/components/auth/auth) to see how to add authentication screens to your app.
