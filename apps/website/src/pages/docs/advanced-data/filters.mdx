---
title: Filters
description: Intuitive data filtering components.
---

<ComponentLinks pro={{ gumroad: true }} />

## Import

- `FilterMenu`: The container component that manages composition.
- `ActiveFilter`: The bare container component.
- `ActiveFilters`: A button with an arrow icon used for navigating to the previous page.
- `FiltersProvider`: A button with an arrow icon used for navigating to the previous page.

```ts
import {
  FilterMenu,
  ActiveFilter,
  ActiveFilters,
  FiltersProvider,
} from '@saas-ui/pro'
```

## Usage

### Basic

```jsx
<FilterMenu />
```

## Props

The `Filter` component and all sub components accept all `Box` properties.

### FilterMenu Props

<PropsTable of="FilterMenu" />
