import Head from 'next/head'

const Meta = () => {
  return (
    <Head>
      <title>Saas UI - The React design system for SaaS products</title>
      <meta
        name="viewport"
        content="minimum-scale=1, initial-scale=1, width=device-width, shrink-to-fit=no, viewport-fit=cover"
      />
    </Head>
  )
}

export default Meta
